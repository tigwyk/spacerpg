from setuptools import setup

setup(
        name='spacerpg',
        packages=['spacerpg'],
        include_package_data=True,
        install_requires=[
            'flask',
            ],
        )

